# 🚀 Инструкция по деплою на Vercel

## 1. Подготовка базы данных

### Создайте PostgreSQL базу (выберите один вариант):

#### Вариант A: Neon (рекомендуется)
1. Перейдите на https://neon.tech
2. Создайте аккаунт и новый проект
3. Скопируйте DATABASE_URL из дашборда

#### Вариант B: Supabase
1. Перейдите на https://supabase.com
2. Создайте новый проект
3. В разделе Settings → Database скопируйте URI

#### Вариант C: Vercel Postgres
1. В дашборде Vercel перейдите в Storage
2. Создайте PostgreSQL базу
3. Скопируйте DATABASE_URL

## 2. Настройка переменных окружения в Vercel

В дашборде вашего проекта на Vercel добавьте эти переменные:

### Обязательные:
```
DATABASE_URL="postgresql://username:password@hostname:port/database"
AUTH_SECRET="T+pWcrtqWsScKFaVSiOwJJ5gdUuGp2J/0krIDtet2YY="
NEXTAUTH_URL="https://your-project-name.vercel.app"
```

### Дополнительные (если используете UploadThing):
```
UPLOADTHING_TOKEN="ваш_токен_uploadthing"
```

## 3. Команды для первого деплоя

```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Войдите в аккаунт
vercel login

# 3. Деплой
vercel --prod

# 4. После деплоя выполните миграции
vercel env pull .env.local
npm run db:migrate:deploy
```

## 4. Команды Prisma для production

Добавьте в package.json:
```json
{
  "scripts": {
    "db:migrate:deploy": "prisma migrate deploy",
    "db:seed:prod": "NODE_ENV=production prisma db seed"
  }
}
```

## 5. Автоматический деплой через GitHub

1. Подключите ваш GitHub репозиторий к Vercel
2. Настройте переменные окружения в дашборде Vercel
3. Каждый push в main ветку будет автоматически деплоиться

## 6. Проверка работоспособности

После деплоя проверьте:
- ✅ Главная страница загружается
- ✅ Регистрация работает
- ✅ Авторизация работает  
- ✅ Добавление слов в словарь
- ✅ Тренировки запускаются

## 7. Troubleshooting

### Ошибка подключения к БД:
- Проверьте правильность DATABASE_URL
- Убедитесь, что IP-адрес Vercel добавлен в whitelist БД

### Ошибки авторизации:
- Проверьте AUTH_SECRET и NEXTAUTH_URL
- Убедитесь, что домен указан правильно

### Ошибки сборки:
- Проверьте, что все зависимости установлены
- Выполните `npm run build` локально для проверки